import { InterviewQuestion } from '../Open Closed Principle';

export default class CSInterviewQuestions implements InterviewQuestion {
  execute(): void {
    console.log('CS questions without the need to modify any other classes');
  }
}
